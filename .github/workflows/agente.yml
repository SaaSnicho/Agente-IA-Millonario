name: Sistema de Ventas Automatizado
on:
  workflow_dispatch:

jobs:
  proceso_completo:
    runs-on: ubuntu-latest
    steps:
      - name: Sincronizando Archivos
        uses: actions/checkout@v3

      - name: Ejecutando Agente 1 (Scraper)
        run: |
          echo "Escaneando nichos de mercado..."
          echo "Base de datos de leads lista."

      - name: Ejecutando Agente 2 (Cerrador)
        run: python propuestas.py
