name: Sistema de Ventas Automatizado
on:
  workflow_dispatch:

jobs:
  proceso_completo:
    runs-on: ubuntu-latest
    steps:
      - name: Sincronizando Archivos
        uses: actions/checkout@v3

      - name: Ejecutando Agente 1 (Scraper Masivo)
        run: echo "Iniciando escaneo global en múltiples ciudades..."

      - name: Ejecutando Agente 2 (Cerrador Multi-Nicho)
        run: python propuestas.py
